function testTesting() {
    const chef = Occupation.get("Chef");
    const softwareDev = Occupation.get("Software Developer")

    Logger.log(chef);
    Logger.log(softwareDev);

    // const week = getWeek();
    //sendScheduledMessage(week);
}